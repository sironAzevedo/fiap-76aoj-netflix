server:  port: 8888

spring:
  application:
    name: netflix-config-server
  cloud:    config:      server:        git:          default-label: main          uri: https://github.com/sironAzevedo/netflix-configurations#          username: ${GITHUB_USER}#          password: ${GITHUB_PASS}
    
