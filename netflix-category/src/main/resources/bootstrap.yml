spring:
  application:
    name: netflix-category
  profiles:
    active: ${ENVIRONMENT:test}
  cloud:
    config:
#      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      uri: ${CONFIG_SERVER_URL:http://netflix-config-server:8888} #Esta url estar apontando para o container docker

management:  endpoints:    web:      exposure:        include: '*'