spring:
  application:
    name: netflix-user
  profiles:
    active: ${ENVIRONMENT:test}
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}

management:  endpoints:    web:      exposure:        include: '*'